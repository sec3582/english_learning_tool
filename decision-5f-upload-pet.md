> ⚠️ 此文件已棄用（2026-07-15）。
> 本文件內容以《香水》電影美學為基礎，該美學框架已於 2026-07-15 全面放棄。
> 現行視覺方向請參閱：`story/world.md`、`.claude/agents/perfume-game-director.md`、`compiled-spec.md` §6。

---

# decision-5f-upload-pet.md — 兩項玩家體驗決策
**角色**：perfume-game-director
**對應階段**：Phase 5e 之後、後續實作之前
**狀態**：決策一（上傳功能）已依討論結果修正並確認；決策二（電子雞）待使用者確認；追加決策三、四已確認存證

---

## 決策一：PDF / 圖片上傳單字功能

### 現況（已讀取 `index.html:1012-1035`）

`匯入文章` 卡片標題列右側塞了一顆 `上傳圖片`（`#ocrPickBtn`，`btn-sm-m` 小按鈕），與下方 `手動輸入` / `網址` 頁籤（`.input-tab`）是兩套不同的視覺系統：頁籤是「內容來源」的一級選擇，上傳圖片按鈕是「附加在標題列的工具」。使用者第一眼看到的是「這是個次要功能」，但它其實是第三種一樣重要的內容來源。

### 判斷

**這不該是次要動作，應該升級成第三個頁籤，與「手動輸入」「網址」平起平坐。**

理由：
1. **使用情境比想像中主流。** 這個工具的使用者常常是「看紙本書/講義中的文章想背單字」——拍照比手動打字更符合真實行為。把它做成小工具按鈕等於在暗示「這是次要功能」，但實際使用頻率可能不輸文字貼上。
2. **它本質上是內容來源，不是頁籤的附加工具。** 手動輸入／網址／圖片-PDF 三者是同一件事的三種輸入管道，資訊架構上應該對稱，不該有一個被摺疊進標題列。
3. **目前位置製造了認知斷裂。** `ocrStatus`／`重跑 OCR` 目前浮在標題列文字說明區，脫離頁籤脈絡，使用者要在「標題列工具」和「頁籤內容」兩套心智模型間切換。

### 具體安排

> **本節「三個頁籤」已被 [決策三](#決策三網址-tab-直接移除) 修正為兩個頁籤（`手動輸入` / `上傳圖片`），此處保留原始討論紀錄不刪改，實際執行以決策三為準。**

- 頁籤改為三個：`手動輸入` / `網址` / `上傳圖片`（PDF 與圖片可共用同一頁籤，因為都走 Gemini vision 擷取路徑，不需要分成兩個頁籤增加選擇負擔）。
- 上傳後的 OCR／擷取文字結果，落回同一個 `articleInput` 可編輯 textarea 呈現（而不是直接送進分析）——原因：Gemini 視覺辨識仍會認錯字，尤其是英文書內斜體、手寫筆記或掃描雜訊，這是背單字工具，一個字認錯就會學錯，所以**必須讓使用者在按下「讓 AI 挑單字」前有機會校對**。
- 共用同一顆主要動作按鈕 `讓 AI 挑單字`，不要為圖片路徑另外做一顆 CTA——避免使用者困惑「兩個分析入口功能是否一樣」。
- 圖片／PDF 辨識的 token 成本通常高於純文字貼上（多一次 vision pass），建議在該頁籤下方加一行小字說明（呼應右上角本月估算 NT$ 的既有機制），例如「圖片辨識較耗用本月額度」，維持使用者對成本的知情權，不需要做成警告樣式，用 `.text-gray-400` 等級即可。
- 視覺份量：與另外兩個頁籤同高、同字重，不特別加 icon 徽章或顏色強調——三者是平權關係，不是「先進功能」。

---

## 決策二：像素電子雞元件

### 現況（已讀取 `js/pixel_pet.js` 與 `index.html:1266-1301`）

這不是純裝飾——它綁定了核心學習迴圈：新增單字＝餵食、完成複習＝獲得 XP、long-idle 會讓 hunger/mood 衰減（拉回訪機制）、等級對應 sprite 進化階段（蛋→小雞→…）。這代表移除它等於移除一段已經在運作的動機／回訪機制，不只是換皮的問題。

### 判斷：**(b) 重新設計成符合世界觀的陪伴元素，且只換皮、不動資料模型**

排除 (a) 移除：會直接砍掉一段有效的行為誘因（衰減機制、等級進度可視化），而美學違和是「皮膚」問題，不是「機制」問題，不需要為了解決前者犧牲後者。

排除 (c) 隔離到次要頁面：把一個「每天回來看它有沒有餓」的機制移出視線，等於自我閹割它存在的目的——這類元件的價值恰好來自「常駐可見」，藏起來後 90% 的行為誘發力會消失。

**選定意象：蠟燭（優先）**，理由：

1. **與世界觀直接對應。** `storyboard.md` CG-3 明訂這個宇宙的材質語彙是「蠟、紙、石頭、皮革」——蠟燭是清單裡本來就該存在的物件，不是外加的比喻。
2. **狀態映射比貓/墨花更自然，改動最小：**
   - `hunger`（飽食度）→ 燭芯剩餘／燭火亮度：飽食度高＝火焰穩定明亮，低＝搖曳將熄
   - `mood`（心情）→ 燭淚流動的整齊度：心情好＝蠟液平整凝結，心情差＝蠟淚雜亂垂掛
   - `xp`／等級 → 蠟燭高度或底座燭台的精緻程度隨等級提升（蛋→小雞→…的進化階梯，換成「新蠟燭→半燃→燭台鑲金邊」之類的漸進形態）
   - 這三個映射都是現有 `petHunger`/`petMood`/`currentXP`/`level` 變數的直接視覺化，**不需要改任何 `pixel_pet.js` 的資料邏輯，只需要重畫 sprite 與命名**。
3. **與工作情境自然共存。** 調香師深夜在作坊工作，燭光是自然存在的光源道具，不會像貓一樣需要額外解釋「為什麼書桌上有隻貓」。

**次要備選：墨花（Suminagashi 墨流）**，理由是 CLAUDE.md 目標 1 已經把 Suminagashi 墨流動畫列為全站視覺主題，若希望電子雞與首頁動畫語彙同源、產生跨元件呼應，墨花也成立——狀態映射會是「墨花在水面展開的複雜度」對應等級、「墨色濃淡」對應心情。缺點是墨流本身已經是動畫系統的角色（見 CLAUDE.md），拿來做常駐 UI 元件可能會和開場動畫搶戲，語意上也比蠟燭抽象（「墨花為什麼會餓」不如「蠟燭為什麼會暗」直覺）。

**排除貓**：這個世界觀裡沒有建立「工作坊裡養貓」的敘事基礎，選它等於繞回「可愛動物吉祥物」的通用套路——正是目前像素電子雞違和的根本原因（一個和世界觀無關的可愛生物），只是換了張皮，沒有解決問題本質。

### 範疇提醒（避免現在做過頭）

CLAUDE.md 目標 6 已經規劃「沉浸式文本小說（失去嗅覺的調香師學徒）」。這代表電子雞未來可能會被整併進小說系統的角色/道具（例如它就是學徒工作桌上的那根蠟燭）。**現階段建議只做視覺重繪＋文案改名，不要現在就設計新的互動機制或跟小說系統掛勾**——等小說系統的敘事需求明朗後再決定是否合併，避免現在投入的美術/邏輯被小說階段的設計推翻。

---

## 決策三：網址 tab 直接移除

### 背景與修正過程

原規劃書 Phase 4.5 已決議移除「網址」輸入頁籤。決策一在討論上傳圖片功能時，出於「三種輸入管道對稱」的資訊架構考量，一度建議把網址頁籤保留下來，與「手動輸入」「上傳圖片」並列成三個頁籤（見上方「具體安排」原始記錄）。

與使用者進一步討論後，**推翻決策一原本的三頁籤建議，回歸原規劃的移除決議**。

### 最終決議

頁籤只保留兩個：`手動輸入` / `上傳圖片`。`網址` 頁籤與其面板（`#inputPanelUrl`，含 `#urlInput` / `#urlFetchBtn` / `#urlStatus`）整組移除。

### 理由與體驗層考量

1. **使用者確認網址擷取功能已無實際需求。** 決策一保留它的理由是「三種來源資訊架構對稱」，這是一個結構性理由，不是使用需求理由——如果實際上沒有人在用網址貼上這條路徑，為了「架構好看」而保留一個沒人用的頁籤，反而是本末倒置。真實使用資料／使用者確認優先於架構對稱性。
2. **兩個頁籤比三個頁籤更聚焦。** 「手動輸入」與「上傳圖片」涵蓋了使用者實際會用的兩種取材方式（打字／拍照）；拿掉網址後，頁籤列本身的掃視成本降低，使用者不需要多花一次判斷「這個網址功能跟我有沒有關係」。這呼應 `VISUAL_SPEC.md §5.1`「首頁唯一視覺焦點，不放置分散注意力的次要元件」的精神——雖然那份規格是為夜模式沉浸式小說寫的，但「減少不必要的選項分支」這個原則在日間模式的匯入文章卡片一樣成立。
3. **與 Phase 4.5 原規劃一致，不是新的範圍縮減。** 這不是臨時砍功能，是把一度被決策一的架構考量暫時扳回來的功能，導正回原本已經決議過的方向，執行上沒有额外風險。

### 對既有文件的影響

- `docs/VISUAL_QA_5E.md` 第 1.2 節「匯入文章卡片」QA 清單裡標注 `#urlInput` 為「過渡期問題，若網址 tab 移除可跳過」——本決策確認移除後，那條 QA 項目可以直接關閉，不需要修色。
- `docs/VISUAL_QA_5E.md` 第 4 節「上傳圖片頁籤」新元件規格本來就是依照「兩頁籤」設計（`#inputTabManual` / `#inputTabUpload`），不需要因為這次補寫決策而修改規格內容——規格文件寫在前、決策文件補寫存證在後，兩者結論一致。

---

## 決策四：「自訂新增單字」搬進 sidebar

### 背景

`#customWordCard` 目前是主內容區「匯入文章」卡片正下方的一張獨立頂層卡片，永遠展開、永遠佔用主內容欄一整張卡片的垂直空間（欄位含查詢列 + 6 個編輯欄位 + 送出按鈕）。

### 最終決議

從主畫面移除 `#customWordCard` 這張獨立卡片，改為摺疊在「單字清單」sidebar（`#wordListCard`）的「開始測驗」按鈕列下方，預設收合，樣式比照既有 `#progressCard` 的摺疊模式（header + chevron + body 展開/收合）。詳細版面規格見 `docs/VISUAL_QA_5E.md` 第 3 節「新元件規格：『自訂新增單字』sidebar 摺疊版」。

### 理由與體驗層考量

1. **這是次要動作，不該佔用主流程的視覺份量。** 對照 `storyboard.md` 的敘事弧線（B2 世界確立 → B7 原料呈現 → B10 深度解析），主畫面的核心動線是「貼文章 → AI 分析 → 存進單字清單」；手動輸入單一單字是這條動線之外的補充功能，使用頻率遠低於文章分析。讓它永遠展開、跟「匯入文章」平起平坐地佔滿一整張卡片，等於用主流程的視覺重量去服務一個次要動作，這正是決策一第 1 點批評「上傳圖片按鈕份量過輕」的相反問題——這裡是「份量給太多」。
2. **摺疊、預設收合，才是份量正確的次要動作處理方式。** 需要用到這個功能的使用者仍然找得到它（在單字清單卡片裡，緊接在最主要的「開始測驗」CTA 下方，位置顯眼、但預設不佔視覺空間）；不需要用到的使用者完全不會被它分心。這跟決策二處理電子雞的邏輯一致——「常駐可見但體積克制」優於「拿掉」或「藏到看不見的地方」，只是這裡的「常駐可見」用摺疊態滿足，而不是像電子雞那樣需要持續呈現數值變化。
3. **位置選在單字清單卡片，而非獨立側欄卡片，是刻意的歸屬設計。** 自訂新增單字的結果，最終都會進到「單字清單」——把入口放在它最終影響的清單卡片裡，比放在主畫面另開一張無關的卡片，更符合「動作與其影響范圍在同一個容器裡」的直覺。
4. **比照 `#progressCard` 既有摺疊模式，而不是發明新機制。** 專案裡已經有一套驗證過的摺疊 pattern（header + chevron + 展開 body），沿用它不需要使用者重新學習一套新的互動語言，也降低實作成本——這跟決策二「這階段只換皮，不擴充機制」的節制原則是同一種判斷方式：能沿用既有系統就不要另外發明。

### 對既有文件的影響

- `docs/VISUAL_QA_5E.md` 第 3 節已依此決議寫好完整版面規格（摺疊態/展開態、欄位改單欄排列、按鈕層級降階），本決策文件只補記「為什麼要搬」的理由存證，版面細節不重複。

---

## 待確認事項

1. ~~上傳圖片是否同意升級為第三頁籤（而非維持標題列按鈕）？~~ **已修正為決策三：兩頁籤（手動輸入／上傳圖片），網址移除。**
2. 電子雞是否同意採用蠟燭意象（或改選墨花）？
3. 是否同意「這階段只換皮，不擴充機制」的範疇邊界？
